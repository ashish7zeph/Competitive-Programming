#include <stdio.h>

int main()
{
    int n,f=1;
    scanf("%d",&n);
    if(n==0)
    printf("1");
    else
    {
        while(n!=0)
        {
            f=f*n--;
        }
        printf("%d",f);
    }
    return 0;
}
